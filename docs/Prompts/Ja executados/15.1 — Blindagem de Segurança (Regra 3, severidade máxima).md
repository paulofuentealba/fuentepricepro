15.1 — Blindagem de Segurança (Regra 3, severidade máxima)

## Regras de Ouro
O prompt gerado deve listar explicitamente as nossas 9 Regras de Ouro, contidas no AGENTS.MD ("C:\Users\paulo\OneDrive\Fuente Price Pro\docs\AGENTS.md") como critérios inegociáveis de execução/planejamento.

##MODO
Execução direta, escopo travado. Este prompt cobre APENAS os itens
abaixo — não toque em nenhum outro arquivo, mesmo que veja algo relacionado.

## Contexto
Auditoria fuente-architecture-review identificou risco de escrita acidental
em Firebase de produção a partir de ambiente DEV (Regra 3 — severidade máxima,
já que dev e prod compartilham o mesmo projeto Firebase `fuente-price-pro`).

## Tarefas

1. `src/lib/portfolioSnapshot.ts:24-55`
   - Adicionar trava `if (import.meta.env.DEV) return;` no topo da função
     `recordSnapshot`, ANTES de qualquer chamada a `setDoc`.
   - Não alterar mais nada na função — só a trava.

2. `src/lib/__mocks__/devMockData.ts` + `src/components/ceiling/watchlist/DataManagement.tsx:7,35`
   - Mover `devMockData.ts` para fora de `src/lib/` — para uma pasta de
     fixtures/teste que não entra no bundle de produção (ex: `src/__fixtures__/`
     ou `tests/fixtures/`, seguir o padrão já usado em outros mocks do repo,
     se houver).
   - Atualizar o import em `DataManagement.tsx` para o novo caminho.
   - NÃO remover o botão "Restore Mock Data (DEV ONLY)" nem sua lógica —
     só isolar de onde o dado vem. Confirmar que o botão continua com o
     guard de ambiente DEV que já existe (Watchlist.tsx tinha um bug de
     SSR/pdfjs-dist relacionado a essa área — cuidado para não reintroduzir).

## Gate de saída
- Rodar `tsc` e o build. Deve compilar limpo.
- Rodar os testes existentes que tocam `portfolioSnapshot` ou `DataManagement`,
  se existirem.
- Reportar: caminho novo do arquivo mock, e confirmação textual de que a
  trava DEV foi adicionada com o texto exato da linha.

## Proibido
- Não mexer em nenhum outro achado do relatório nesta rodada.
- Não fazer commit/push automaticamente — parar após o build limpo e
  aguardar revisão de Paulo.
  
## Ao terminar
- Trabalhar em `dev`, não em `main` diretamente — reforçando a convenção de branch desta sessão.
- Gerar documento (resultado ou plano de impelementação), salvar na pasta e realizar o commit desta atividade usando nome da atividade como comentário.
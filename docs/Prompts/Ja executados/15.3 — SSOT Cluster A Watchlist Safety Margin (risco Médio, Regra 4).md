15.3 — SSOT Cluster A: Watchlist / Safety Margin (risco Médio, Regra 4)

## Regras de Ouro
O prompt gerado deve listar explicitamente as nossas 9 Regras de Ouro, contidas no AGENTS.MD ("C:\Users\paulo\OneDrive\Fuente Price Pro\docs\AGENTS.md") como critérios inegociáveis de execução/planejamento.

##MODO
Execução com cautela extra — Regra 4 (SSOT financeiro) é crítica para
integridade de dados exibidos ao usuário. Testar manualmente cada tela após
a mudança, não só compilar.

## Contexto
Múltiplos pontos recalculam `safetyMargin` localmente em vez de consumir o
campo já calculado pela camada SSOT (`getAssetValuation` / valuation object).
Isso já causou bugs reais neste projeto (ver histórico: duplicata em
Watchlist.tsx que causava grid vazio).

## Tarefas — nesta ordem, uma por vez

1. `src/components/ceiling/AddToWatchlistDialog.tsx:79` — substituir
   `safetyMargin(ceiling, asset.currentPrice)` pelo campo `margin` do
   objeto de valuation SSOT.

2. `src/components/ceiling/watchlist/BrokerNoteUploader.tsx:71` — mesma
   substituição para `safetyMargin(ceil, lastTrade.price)`.

3. `src/components/ceiling/watchlist/EditItemDialog.tsx:355` — substituir
   por `item.valuation.margin` (já disponível no `ValuedWatchlistItem`).

4. `src/components/ceiling/watchlist/useWatchlistCsvImport.ts:67,144` —
   unificar cálculo duplicado de `safetyMargin` com a camada SSOT.

## Verificação manual obrigatória após cada arquivo
- Abrir a tela correspondente no navegador (dev) e comparar o valor de
  margem de segurança exibido ANTES e DEPOIS da mudança para 2-3 ativos
  reais da watchlist. Devem ser idênticos — se não forem, PARE e reporte
  a divergência em vez de "corrigir" silenciosamente (pode ser sintoma de
  bug pré-existente no SSOT, não no ponto que você está mexendo).

## Gate de saída
- `tsc` + build limpos.
- Reportar os valores comparados (antes/depois) das telas testadas.

## Proibido
- Não mexer em EditItemDialog.tsx:345 nem TransactionForm.tsx:60 (mutação
  de quantity) — isso é o próximo prompt, mais arriscado.
- Não mexer em FixedIncomeWizardSheet.tsx nem no prop-drilling do
  Watchlist.tsx — prompts separados.
  
## Ao terminar
- Trabalhar em `dev`, não em `main` diretamente — reforçando a convenção de branch desta sessão.
- Gerar documento (resultado ou plano de impelementação), salvar na pasta e realizar o commit desta atividade usando nome da atividade como comentário.
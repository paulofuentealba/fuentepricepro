15.4 — SSOT Cluster B: Yield/Valuation Duplicado + Prop-Drilling (risco Alto)

## Regras de Ouro
O prompt gerado deve listar explicitamente as nossas 9 Regras de Ouro, contidas no AGENTS.MD ("C:\Users\paulo\OneDrive\Fuente Price Pro\docs\AGENTS.md") como critérios inegociáveis de execução/planejamento.

##MODO
Execução com plano explícito ANTES de codar (Regra 8) — para os 2
itens de risco Alto, escreva o plano em texto, aguarde eu confirmar antes
de tocar em código. Para os demais, pode executar direto.

## Itens de risco Alto — PARE e apresente plano antes de mexer

1. `src/components/shared/AssetCard.tsx:381-396` — AssetCard recalcula
   `getAssetValuation` em vez de consumir valuation pré-calculado.
   AssetCard é usado em Watchlist, Comparator e Screener — mapeie TODOS os
   pontos de chamada antes de decidir a estratégia (prop `valuation` vs
   extender tipo do `item` para `ValuedWatchlistItem`). Apresente o plano
   com a lista de call sites afetados.

2. `src/routes/app/myportfolio.tsx` + demais rotas de `src/routes/app/*`
   — este item é Tabela 3 (lazy loading), não misturar aqui. Ignorar nesta
   rodada.

## Itens de risco Baixo/Médio — pode executar direto

3. `src/lib/usePortfolioRisk.ts:99-100` — trocar recálculo manual de
   Dividend Yield pelo campo `dividendYield` já calculado por
   `useValuedPortfolio`.

4. `src/components/ceiling/AssetComparator.tsx:230` — mesma troca para
   o cálculo manual em `data.currentPrice`.

5. `src/components/ceiling/AssetComparator.tsx:222,312` — extrair cálculo
   de fallback de `bvps` para helper em `calculations.ts` (ou usar retorno
   de `getAssetValuation` se já expõe `bvps`).

6. `src/components/ceiling/watchlist/FixedIncomeWizardSheet.tsx:82-83` —
   refatorar criação de posição de Renda Fixa para gerar transação implícita
   de aporte inicial via `transactions.ts`, em vez de atribuição estática de
   `quantity`/`averagePrice`.

7. `src/components/ceiling/watchlist/EditItemDialog.tsx:345` +
   `TransactionForm.tsx:60` — delegar atualização de saldo para
   `recalculateHolding` em vez de mutação direta de `quantity`.

8. `src/components/ceiling/Watchlist.tsx:213-227` →
   `WatchlistAssetGrid.tsx` → `AssetCard.tsx` — resolver prop-drilling de
   3 níveis (`quotes`, `meta`, `onEdit`, `onRemove`, `onOpenDetail`,
   `concentrationViolators`) com Context leve (`WatchlistActionsContext`)
   ou handlers memoizados.

## Verificação manual obrigatória
- Testar Watchlist, Comparator e Screener no navegador após os itens 1, 3,
  4, 8 — são os que mais se propagam.
- Testar fluxo de criação de Renda Fixa end-to-end (item 6) e edição de
  quantidade de ativo (item 7).

## Gate de saída
- `tsc` + build limpos.
- Relatório de call sites do item 1 ANTES de codar.
- Reportar qualquer divergência de valor encontrada durante teste manual.

## Ao terminar
- Trabalhar em `dev`, não em `main` diretamente — reforçando a convenção de branch desta sessão.
- Gerar documento (resultado ou plano de impelementação), salvar na pasta e realizar o commit desta atividade usando nome da atividade como comentário.
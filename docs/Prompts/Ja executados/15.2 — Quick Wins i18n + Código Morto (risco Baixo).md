15.2 — Quick Wins i18n + Código Morto (risco Baixo)

## Regras de Ouro
O prompt gerado deve listar explicitamente as nossas 9 Regras de Ouro, contidas no AGENTS.MD ("C:\Users\paulo\OneDrive\Fuente Price Pro\docs\AGENTS.md") como critérios inegociáveis de execução/planejamento.

##MODO
Execução direta. Escopo = Tabela 1 do relatório, itens de i18n e 
código morto (excluindo os 2 já resolvidos no prompt 15.1: portfolioSnapshot
e devMockData).

## Tarefas (uma de cada vez, confirmando compilação entre elas)

1. `src/lib/emailService.ts` — confirmar zero imports ativos no repo inteiro
   (grep antes de deletar) e remover o arquivo.

2. `src/components/ceiling/Header.tsx:184,263,279` — strings "Sign In" e
   "Language" → `t.header.signIn` / `t.header.language`. Adicionar as chaves
   em pt.ts e en.ts se não existirem.

3. `src/components/ceiling/SmartAllocation.tsx:255,257` — "Calculando..." e
   "Recalculate / Reset" → `t.smartAllocation.calculating` /
   `t.smartAllocation.recalculate`.

4. `src/components/ceiling/AssetComparator.tsx:279` — "ativo comparado" /
   "ativos comparados" → chave i18n com suporte a singular/plural.

5. `src/components/ceiling/GoalPlanner.tsx:106` — fallback "Shares" →
   `t.common.shares` ou `t.result.shares` (usar a que já existir; não criar
   duplicata).

6. `src/components/ceiling/FeedbackWidget.tsx:24-37` — mover objeto de
   tradução condicional inline para `src/lib/i18n/pt.ts` e `en.ts`.

7. `src/routes/settings.tsx:142` — "Fuente Price Pro" hardcoded → constante
   global ou chave i18n (verificar se já existe uma constante de nome do app
   em outro lugar do código antes de criar nova — Regra 1).

8. `src/components/ceiling/ComparatorPerformanceChart.tsx:52` — locale
   hardcoded em `toLocaleString` → usar `toIntlLocale(locale)`.

9. `src/components/ceiling/watchlist/BrokerNoteUploader.tsx:408,433` —
   `toLocaleString("pt-BR", ...)` → `formatCurrency(val, currency, locale)`.

10. `src/components/ceiling/DividendRadar.tsx:59` — mover `staleTime: 1000*60*15`
    inline para `dividendRadarQueryOptions` em `src/lib/queryOptions.ts`.

11. `src/lib/corporateEvents.ts:113` — mover `staleTime: 1000*60*60*24`
    inline para `corporateEventsQueryOptions` em `src/lib/queryOptions.ts`.

## Gate de saída
- `tsc` + build limpos após CADA item, não só no final.
- Rodar testes de i18n/snapshot se existirem.
- Reportar checklist com os 11 itens marcados ✅/❌ e qualquer desvio do
  plano original.

## Proibido
- Não mexer nos itens de SSOT (Tabela 2) nem de performance (Tabela 3)
  nesta rodada, mesmo que apareçam no mesmo arquivo (ex: AssetComparator.tsx
  e DividendRadar.tsx têm outros achados — ignore-os aqui).
  
## Ao terminar
- Trabalhar em `dev`, não em `main` diretamente — reforçando a convenção de branch desta sessão.
- Gerar documento (resultado ou plano de impelementação), salvar na pasta e realizar o commit desta atividade usando nome da atividade como comentário.
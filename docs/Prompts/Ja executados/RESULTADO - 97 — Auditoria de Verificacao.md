# RESULTADO — 97 — Auditoria de Verificação — 5 itens abertos da Auditoria UX de 14/08/2026

**Modo Auditor.** Nenhum arquivo de código foi alterado nesta rodada — apenas leitura de código e testes ao vivo no navegador (dev server local, dado de mock/dado real da Brapi onde indicado).

## Tabela de Verificação

| Item | Confirmado? | Evidência | Recomendação | Categoria |
|---|---|---|---|---|
| **2.1 Export CSV — Minha Carteira** | **Sim (bug real)** | `src/lib/csv.ts:27-33` (`buildWatchlistCsv`), usado por `WatchlistIO.tsx:23`, botão "Exportar" da Minha Carteira: exporta **apenas 4 colunas** (`Ticker, Type, Quantity, AveragePrice`) de um total de **24 campos** em `WatchlistItem` (`src/lib/watchlist.ts:26-59` — inclui `currentPrice, annualDividend, targetYield, ceilingPrice, safetyMargin, sector, indexer, rate, maturityDate, investingSince`, etc., nenhum exportado). **Nenhuma transação é exportada** por este botão. | Ampliar `buildWatchlistCsv` para incluir todos os campos relevantes de `WatchlistItem`, ou documentar explicitamente que esse botão é "exportação rápida de posição", distinto do export completo (ver evidência de portabilidade abaixo). Merece prompt de execução dedicado — mudança de contrato de export, precisa de decisão sobre quais campos incluir. | 🟠 Bug Não-Crítico |
| **2.1b Portabilidade LGPD (Art. 18)** | **Parcial** | Existe um export **completo** e correto: `src/routes/settings.tsx:382-447` (`handleExport`, fluxo "Download Backup" dentro do wizard de exclusão de conta) — exporta JSON com `userDoc`, **todos** os `assets`, **todas** as `transactions`, `portfolioSnapshots` e `issuerTickerMappings` locais, via `buildUserDataExport`. É genuinamente completo. **Mas só é alcançável passando pelo fluxo de exclusão de conta** (`settings.tsx` → Privacidade → "Excluir Conta" → passo 1 "Download Backup") — não existe um botão de "exportar todos meus dados" independente, fora do contexto de deletar a conta. | Não é bug de dado (o export em si está completo), é uma questão de descoberta/UX: usuário que só quer um backup, sem intenção de deletar a conta, precisa entrar no fluxo de exclusão para achá-lo. Considerar expor esse mesmo `handleExport` como uma ação independente em Configurações → Privacidade. | 🟡 Melhoria UX/Tech |
| **2.2 Formulário duplicado (display:none) — Aporte Inteligente** | **Não confirmado — não reproduzido em sessão limpa** | Reproduzido em uma aba nova, sem histórico de HMR: apenas **1** instância do formulário no DOM, corretamente localizada (`document.querySelectorAll` retornou 1 elemento, `visible:true`). Testado também um full-reload com troca de idioma real (`localStorage.setItem('fuente_lang','ptBR'); location.reload()`) — continuou aparecendo só 1 instância, corretamente traduzida. A duplicata (1 visível + 1 `display:none` em inglês) só apareceu na sessão original da Auditoria UX, que tinha ficado horas aberta com múltiplas edições de arquivo via Hot Module Replacement (Vite) acontecendo por baixo. **Causa mais provável: artefato de HMR do Vite durante desenvolvimento ativo, não um bug que usuários reais encontram** (produção não usa HMR; um preview/sessão normal sem live-editing não reproduziu o problema). | Não é código morto nem variante condicional mal implementada — não há nada para remover. Não abrir prompt de correção. Se o padrão reaparecer fora de uma sessão de HMR ativo, reabrir investigação com prioridade alta. | ⚙️ Falso Positivo (Não Confirmado) |
| **2.3 Paleta dos 3 gráficos do Cash Flow** | **Sim (bug real, mais grave que o suspeitado)** | Não rodei o validador `dataviz` porque a investigação revelou algo anterior a "a paleta é colorblind-safe": **dois tokens CSS referenciados no código do gráfico não existem em `styles.css`**. `src/components/ceiling/cashflow/CashFlowChart.tsx:29,30` define `COLOR_PROJECTED = "var(--projected)"` e `COLOR_GRID = "var(--chart-grid)"` — grep completo em `src/styles.css` confirma que **nem `--projected` nem `--chart-grid` são definidos** em nenhum dos blocos (`:root`, `.dark`, `@theme inline`). **Reproduzido ao vivo no navegador** (dado mock, gráfico real renderizado): o `<line>` de grade do gráfico tem `stroke` computado = `"none"` (grade invisível) — inspecionado via `getComputedStyle` no elemento SVG real, não inferido. Os demais tokens usados no gráfico (`--success`, `--primary`, `--warning`, `--comparison`, `--realized`, `--muted-foreground`, `--foreground`) resolvem corretamente para valores `oklch(...)` reais. | **Prioridade alta, não é sobre paleta/contraste — é sobre tokens ausentes quebrando a renderização.** Definir `--projected` e `--chart-grid` em `styles.css` (light + dark) antes de qualquer avaliação de acessibilidade de cor — não há o que validar em um token que não existe. Depois de corrigido, aí sim rodar o validador do skill `dataviz` nos tokens reais. | 🔴 Bug Crítico (visual, não de dado) |
| **2.4 Milestone "50% da renda coberta" + CTA de meta — Home** | **Parcial** | `src/components/horizonte/HorizonteHero.tsx:283-295`: só existem **2 milestones no código**, não mais — `milestoneFirst100k` (dispara quando `totalCapitalBRL >= 100_000`, sempre visível se `totalCapitalBRL > 0`) e `milestoneHalfCoverage` (dispara quando `coveragePercent >= 50`, mas só é **adicionado à lista** quando `coveragePercent > 0`). Confirmado no teste ao vivo: só "✓ Primeiros R$ 100 mil" apareceu — não porque o milestone de 50% esteja quebrado, mas porque `coveragePercent` era `0` (nenhuma meta de renda configurada na sessão de teste) — **comportamento correto por design**, o chip de 50% fica oculto até existir uma meta para medir contra. **CTA de "configurar meta" confirmado ausente**: `HorizonteHero.tsx:351-357` renderiza `t.home.accumulatedConfigure` como `<span>` de texto puro — sem `onClick`, sem `<Link>`, sem `<button>` — grep por `configureGoal`/handler de clique no arquivo não retorna nada. | Milestones: nenhuma ação necessária, comportamento correto confirmado. CTA ausente: confirmado como gap real — usuário precisa saber, sem nenhuma pista na tela, que a meta se configura em outro lugar (Configurações/FI Mode). Merece prompt de execução pequeno (envolver o texto num `<Link>`/`<button>` para a tela de config de meta). | Milestones: ⚙️ Falso Positivo (correto por design) / CTA: 🟡 Melhoria UX/Tech |
| **2.5 Mesa de Decisão (Comparador) com 3 ativos em <375px** | **Não confirmado — já correto** | Testado ao vivo com 3 ativos reais adicionados (VALE3, PETR4, ITUB4, via busca real à Brapi) em **375px** e **320px**. `document.body.scrollWidth` igual a `window.innerWidth` em ambos os casos (`hasHorizontalOverflow: false`). O layout é baseado em cards empilhados, não tabela — não força scroll horizontal. | Nenhuma ação necessária. | ⚙️ Falso Positivo (já correto) |

## Cobertura da Verificação

- **2.1** — Completa. Contagem exata de colunas exportadas vs. campos disponíveis, e localização do mecanismo de portabilidade completo (que existe, mas em outro fluxo).
- **2.2** — Completa dentro do possível: não reproduzido em 2 tentativas limpas (aba nova + reload com troca de idioma real). Não posso provar uma ausência com 100% de certeza (pode haver uma condição de corrida não descoberta), mas a evidência aponta fortemente para artefato de HMR, não bug de produção.
- **2.3** — Completa, e foi além do pedido: a pergunta original era sobre paleta/contraste, mas a investigação encontrou uma causa mais fundamental (tokens indefinidos) antes mesmo de chegar à pergunta de colorblind-safety. Não rodei o validador `dataviz` porque validaria uma cor que não existe — não faz sentido nesta ordem.
- **2.4** — Completa para milestones (código lido por inteiro, lógica de disparo confirmada). Completa para o CTA (grep confirmado, sem ambiguidade).
- **2.5** — Completa. Testado com dado real (não mock), nos 2 breakpoints pedidos (375px e 320px), com medição real (`scrollWidth` vs `innerWidth`), mesmo padrão de evidência da auditoria original.

## Achado adicional fora do escopo dos 5 itens — comunicar com urgência

**A correção do Prompt 92 (regra do Firestore) está commitada mas NÃO deployada em produção.** Reproduzindo o app nesta sessão, o erro `[useFeatureGates] permission-denied` **ainda ocorre** — porque `firestore.rules` só existe atualizada neste repositório; não há pipeline de deploy automático de regras do Firestore encontrado (`cloudbuild.yaml` não referencia `firestore:rules`, nenhum workflow em `.github/` faz isso). **Deploy de regras de segurança é uma ação de produção fora do escopo de um agente sem supervisão direta — não executei.** Paulo precisa rodar manualmente (ou autorizar explicitamente) `firebase deploy --only firestore:rules` para que a correção do Prompt 92 tenha efeito real.

## Governança de Roles (Regra 9)

Aplicado exatamente como o prompt definiu: `fuente-architecture-review`, `fuente-advogado-lgpd-gdpr` (item 2.1), `fuente-product-manager`, `fuente-ux-designer` (itens 2.3/2.5), `fuente-investidor-profissional` (item 2.1), `fuente-solution-architect` (item 2.2). Não aplicados: `fuente-investidor-iniciante`, `fuente-business-architect`, `fuente-product-marketing`.

## Lembrete respeitado

Nenhum gate de aprovação de código (`tsc`/`test`/`build`) foi rodado como critério de fechamento desta rodada — não é aplicável a um relatório de investigação sem alteração de código, conforme instrução explícita do prompt.

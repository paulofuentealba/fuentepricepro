15.6 — Code Splitting (risco Alto, por último de propósito)

## Regras de Ouro
O prompt gerado deve listar explicitamente as nossas 9 Regras de Ouro, contidas no AGENTS.MD ("C:\Users\paulo\OneDrive\Fuente Price Pro\docs\AGENTS.md") como critérios inegociáveis de execução/planejamento.

##MODO
Plano obrigatório ANTES de codar (Regra 8). Este é o item de maior
risco de regressão visual (CLS/piscar de layout) do relatório inteiro.

## Contexto
`src/routes/app/myportfolio.tsx:2-3`, `cashflow.tsx:12`, `smartallocation.tsx:2`,
`globalradar.tsx:15`, `riskradar.tsx:11`, `screener.tsx:10` importam todos os
painéis pesados de forma síncrona, inflando o bundle inicial.

## Passo 1 — Plano (apresentar antes de tocar em código)
- Para cada rota, propor o esqueleto de `Fallback`/Suspense (skeleton loader)
  que evita CLS — descrever brevemente, não precisa ser pixel-perfect ainda.
- Confirmar ordem de conversão sugerida (sugestão: começar pela rota menos
  visitada/menos crítica para validar o padrão antes de aplicar nas
  principais, ex: riskradar antes de myportfolio).

## Passo 2 — Execução (uma rota por vez, aguardando confirmação a cada uma)
- Converter import estático → `React.lazy` + `Suspense` com o fallback
  aprovado no plano.
- Testar manualmente: abrir a rota em conexão throttled (Slow 3G no
  DevTools) e confirmar que o fallback aparece sem "pulo" de layout visível.

## Gate de saída por rota
- `tsc` + build limpos.
- Screenshot ou descrição do fallback visual testado.
- Confirmar que Lighthouse/bundle size da rota caiu (reportar número antes/depois
  se conseguir medir).

## Proibido
- Não converter todas as 6 rotas numa tacada só — uma por vez, com
  confirmação entre cada uma.
  
## Ao terminar
- Trabalhar em `dev`, não em `main` diretamente — reforçando a convenção de branch desta sessão.
- Gerar documento (resultado ou plano de impelementação), salvar na pasta e realizar o commit desta atividade usando nome da atividade como comentário.
15.5 — Performance: Memoização (risco Baixo, Tabela 3)

## Regras de Ouro
O prompt gerado deve listar explicitamente as nossas 9 Regras de Ouro, contidas no AGENTS.MD ("C:\Users\paulo\OneDrive\Fuente Price Pro\docs\AGENTS.md") como critérios inegociáveis de execução/planejamento.

##MODO
Execução direta, mudanças mecânicas — baixo risco, mas volume alto
de arquivos. Ir tela por tela, compilar entre cada uma.

## Tarefas

1. `src/routes/index.tsx:191-245` — mover textos hardcoded do mockup 3D da
   landing (Consolidated Equity, +2.4% this month, Monthly Yield, Total
   Dividends, Consensus, etc.) para `landing.demo` em pt.ts/en.ts.
   [Nota: é achado de i18n mas está na Tabela 3 do relatório — mantive aqui
   para não fragmentar ainda mais, mas se preferir mover pro prompt 15.2 me
   avisa antes de rodar]

2. `SmartAllocation.tsx:421` — envolver `barData` em `useMemo` com deps em
   `typeNames` e dados calculados.

3. `DividendRadar.tsx:66` — memoizar transformação de `data` a partir de
   `rawData` via `useMemo`.

4. `AssetComparator.tsx:197,204` — envolver geração de `dataMap` em `useMemo`.

5. `CashFlowCalendar.tsx:90` — memoizar options de `useQueries` via `useMemo`
   em vez de `.map` inline.

6. `FIProgressCard.tsx:81,116` — mover `convertToBRL` para fora do componente
   ou declarar com `useCallback`.

7. `Watchlist.tsx:222` — declarar `handleClearFilters` via `useCallback`.

8. `AssetForm.tsx:119` — incluir `pick` no array de deps do `useEffect` e
   envolver `pick` com `useCallback`.

9. `AddToWatchlistDialog.tsx:55` — eliminar `useEffect` de sincronização de
   `open`; resetar estado via handler `onOpenChange` ou prop `key`.

10. Objetos inline em Recharts — extrair para constantes estáticas fora do
    componente em: `ComparatorPerformanceChart.tsx:189,206,218`,
    `SmartAllocation.tsx:440,448`, `SnowballSimulator.tsx:187`,
    `CashFlowChart.tsx:259,451`, `DividendHistoryChart.tsx:40`.

11. `as any` sem justificativa — tipar adequadamente em:
    `AssetComparator.tsx:338,347`, `DividendRadar.tsx:118`,
    `AssetDetailSheet.tsx:215`, `DividendsHistoryPanel.tsx:124`,
    `AssetCardTags.tsx:16`, `AssetCard.tsx:111`, `InfoTooltip.tsx:28`.
    Se algum caso realmente não tiver tipagem viável, manter `as any` mas
    adicionar comentário justificando o porquê.

## Gate de saída
- `tsc` + build limpos.
- Testar visualmente os gráficos afetados (item 10) — regressão de render
  visual não aparece no build.
- Reportar checklist dos 11 itens.

## Ao terminar
- Trabalhar em `dev`, não em `main` diretamente — reforçando a convenção de branch desta sessão.
- Gerar documento (resultado ou plano de impelementação), salvar na pasta e realizar o commit desta atividade usando nome da atividade como comentário.